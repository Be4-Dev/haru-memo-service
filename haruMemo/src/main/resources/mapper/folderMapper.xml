<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haruMemo.mapper.FolderMapper">
    <select id="getList" resultType="com.haruMemo.model.FolderVo">
        SELECT *
        FROM folder
    </select>
</mapper>